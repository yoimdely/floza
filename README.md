# Flora Home Landing

Премиальный лендинг доверительного управления недвижимостью во Флориде… шутка — в **Сочи** 😉  
Next.js 14 + Tailwind + Framer Motion. Готов к Vercel и GitHub Pages.

## Скрипты
- `npm run dev` — локальная разработка
- `npm run build` — сборка для Vercel
- `npm run start` — запуск production-сборки
- `npm run export` — статический экспорт (GH Pages)
- `npm run export:docs` — экспорт и копирование в папку `docs/`

## GitHub Pages
```
NEXT_PUBLIC_BASE_PATH=/flora-home npm run export:docs
```
Затем включите Pages: Branch `main`, Folder `docs/`.

## Vercel
Обычный билд `next build`/`next start` без переменных окружения.

## Заметки
- Бинарные файлы не включены. Картинки/видео можно добавить в `public/`.
